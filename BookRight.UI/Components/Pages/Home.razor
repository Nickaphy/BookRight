@page "/"

<PageTitle>Home</PageTitle>

@code {
    [Inject] private NavigationManager Nav { get; set; } = default!;

    protected override void OnInitialized()
    {
        Nav.NavigateTo("/create-booking", replace: true);
    }
}
